const logPath = '/var/log/test/',
    options = {
        logLevel : 7,
        display: true,
        logFile : logPath + 'nice.log',
        errorLogFile : logPath + 'error.log'
    },
//  log = require('./log.class.js').init(options);
    log = require('../lib/log.class').getInstance(true);

log.emerg('emerg - dont use that');
log.alert('alert - not nice');
log.crit('crit - more imporant');
log.err('err - imporant');
log.warning('warning message');
log.notice('notice data');
log.info('info text');
log.debug('debug info');

log.dailyLog(logPath + 'daily.log', 'content for daily file');
log.writeLog(logPath + 'requests.log', 'GET ....');


var obj = { etcd: { etcdHost: '127.0.0.1', port: 2379 } };
log.info(obj);


log.plain('hallo,'.gray +  'wie gehts'.green);
